import React from 'react'
import './styles.css'

const Build = React.createClass({
  render: function() {
    return(
      <h2 className="build">The Build Page</h2>
    )
  }
})

export default Build
